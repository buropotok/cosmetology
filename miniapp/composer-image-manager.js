import {moveItem,translatedActiveIndex} from './composer-image-order.js';

(()=>{
const input=document.querySelector('#image'),previews=document.querySelector('#previews'),removeAll=document.querySelector('#remove-image'),status=document.querySelector('#status');
if(!input||!previews)return;
let files=Array.from(input.files||[]).slice(0,10),internalChange=false,wideCheckGeneration=0,telegramLayout='slideshow',dragState=null,inputFilesShadowed=false;
const VK_MAX_ASPECT=16/9;
const telegramLayoutRow=document.createElement('div');telegramLayoutRow.className='composer-telegram-photo-layout';telegramLayoutRow.hidden=true;telegramLayoutRow.style.cssText='display:flex;align-items:center;justify-content:space-between;gap:12px;margin-top:10px;padding:11px 12px 1px;border-top:1px solid #e5e5ea;color:#6e6e73;font-size:14px;line-height:1.2';
const telegramLayoutLabel=document.createElement('span');telegramLayoutLabel.textContent='Отображение фото в Telegram:';
const telegramLayoutButton=document.createElement('button');telegramLayoutButton.type='button';telegramLayoutButton.className='composer-telegram-layout-toggle';telegramLayoutButton.style.cssText='flex:0 0 auto;border:0;border-radius:9px;background:#e8f3fb;color:#2481cc;padding:8px 12px;font:600 14px/1.2 inherit';telegramLayoutButton.textContent='Карусель';telegramLayoutButton.setAttribute('aria-label','Отображение фото в Telegram: Карусель. Нажмите, чтобы выбрать коллаж.');telegramLayoutRow.append(telegramLayoutLabel,telegramLayoutButton);removeAll?.after(telegramLayoutRow);
function updateTelegramLayoutVisibility(){telegramLayoutRow.hidden=files.length<2}
function renderTelegramLayout(){const isCollage=telegramLayout==='collage';telegramLayoutButton.textContent=isCollage?'Коллаж':'Карусель';telegramLayoutButton.setAttribute('aria-label',`Отображение фото в Telegram: ${isCollage?'Коллаж':'Карусель'}. Нажмите, чтобы выбрать ${isCollage?'карусель':'коллаж'}.`)}
function setTelegramLayout(value){telegramLayout=value==='collage'?'collage':'slideshow';renderTelegramLayout()}
telegramLayoutButton.addEventListener('click',()=>setTelegramLayout(telegramLayout==='slideshow'?'collage':'slideshow'));
function shadowInputFiles(){try{Object.defineProperty(input,'files',{configurable:true,get:()=>files});inputFilesShadowed=true;return true}catch{return false}}
function clearInputFilesShadow(){if(!inputFilesShadowed)return;try{delete input.files}catch{}inputFilesShadowed=false}
function syncInput(){clearInputFilesShadow();if(typeof DataTransfer!=='undefined'){try{const dt=new DataTransfer();files.forEach(file=>dt.items.add(file));input.files=dt.files;return true}catch{}}return shadowInputFiles()}
function notifyChange(){updateTelegramLayoutVisibility();void updateVkAspectWarning();syncInput();internalChange=true;try{input.dispatchEvent(new Event('change',{bubbles:true}))}finally{internalChange=false}}
function showLimit(){if(!status)return;status.textContent='Можно выбрать не больше 10 фотографий. Будут использованы первые 10.';status.className='error'}
function readImageSize(file){return new Promise(resolve=>{const url=URL.createObjectURL(file),img=new Image();const done=value=>{URL.revokeObjectURL(url);resolve(value)};img.onload=()=>done({width:img.naturalWidth,height:img.naturalHeight});img.onerror=()=>done(null);img.src=url})}
function formatIndexes(indexes){if(indexes.length===1)return `Изображение №${indexes[0]}`;if(indexes.length===2)return `Изображения №${indexes[0]} и №${indexes[1]}`;return `Изображения ${indexes.slice(0,-1).map(n=>`№${n}`).join(', ')} и №${indexes[indexes.length-1]}`}
async function updateVkAspectWarning(){const generation=++wideCheckGeneration,current=files.slice(),sizes=await Promise.all(current.map(readImageSize));if(generation!==wideCheckGeneration)return;const wide=[];sizes.forEach((size,index)=>{if(size?.height>0&&size.width/size.height>VK_MAX_ASPECT+0.001)wide.push(index+1)});let warning=document.querySelector('#vk-aspect-warning');if(!wide.length){warning?.remove();return}if(!warning){warning=document.createElement('p');warning.id='vk-aspect-warning';warning.setAttribute('role','status');warning.style.cssText='margin:8px 0 0;padding:10px 12px;border-radius:10px;background:rgba(255,149,0,.14);color:#b35a00;font-size:14px;line-height:1.35';previews.parentElement?.append(warning)}warning.textContent=`${formatIndexes(wide)} шире 16:9. ВКонтакте обрежет ${wide.length===1?'его':'их'} по краям.`}
function addFiles(incoming){const next=Array.from(incoming||[]),total=files.length+next.length;files=[...files,...next].slice(0,10);notifyChange();if(total>10)showLimit()}
function replaceAt(index,file){if(!Number.isInteger(index)||index<0||index>=files.length||!(file instanceof File))return false;files[index]=file;notifyChange();return true}
function moveFile(from,to){const next=moveItem(files,from,to);if(!next)return false;const state=window.CosmoComposerState,active=state?.getSnapshot?.().activePhotoIndex;files=next;if(Number.isInteger(active))state?.setActivePhotoIndex?.(translatedActiveIndex(active,from,to));notifyChange();return true}
window.CosmoComposerImages={addFiles,replaceFiles(incoming){files=Array.from(incoming||[]).slice(0,10);notifyChange()},replaceAt,moveFile,getFiles(){return files.slice()},getTelegramLayout(){return telegramLayout},setTelegramLayout};
input.addEventListener('change',event=>{if(event.isTrusted)clearInputFilesShadow()},true);
input.addEventListener('change',event=>{if(internalChange)return;const incoming=Array.from(input.files||[]).slice(0,10);if(event.isTrusted)addFiles(incoming);else{files=incoming;internalChange=true;try{input.dispatchEvent(new Event('change',{bubbles:true}))}finally{internalChange=false}updateTelegramLayoutVisibility();void updateVkAspectWarning()}});
removeAll?.addEventListener('click',()=>{files=[];notifyChange()});
function clearDropTarget(){previews.querySelectorAll('.composer-thumb.is-drop-target').forEach(node=>{node.classList.remove('is-drop-target');node.style.outline=''})}
function endDrag(event,commit){if(!dragState||event.pointerId!==dragState.pointerId)return;const {handle,from,to,wrap}=dragState;dragState=null;wrap.style.opacity='';clearDropTarget();try{handle.releasePointerCapture?.(event.pointerId)}catch{}if(commit&&from!==to)moveFile(from,to)}
function startDrag(event,wrap,handle){if(files.length<2||event.button>0)return;const wraps=[...previews.querySelectorAll('.composer-thumb')],from=wraps.indexOf(wrap);if(from<0||from>=files.length)return;event.preventDefault();dragState={pointerId:event.pointerId,from,to:from,handle,wrap};handle.setPointerCapture?.(event.pointerId);wrap.style.opacity='.65';const cleanup=()=>{handle.removeEventListener('pointerup',onUp);handle.removeEventListener('pointercancel',onCancel)},onUp=endEvent=>{cleanup();endDrag(endEvent,true)},onCancel=endEvent=>{cleanup();endDrag(endEvent,false)};handle.addEventListener('pointerup',onUp);handle.addEventListener('pointercancel',onCancel)}
function moveDrag(event){if(!dragState||event.pointerId!==dragState.pointerId)return;event.preventDefault();const target=docElementFromPoint(event.clientX,event.clientY)?.closest?.('.composer-thumb');if(!target||!previews.contains(target))return;const wraps=[...previews.querySelectorAll('.composer-thumb')],to=wraps.indexOf(target);if(to<0||to>=files.length)return;dragState.to=to;clearDropTarget();target.classList.add('is-drop-target');target.style.outline='2px solid #2481cc'}
function docElementFromPoint(x,y){return document.elementFromPoint?.(x,y)||null}
function decorate(){const images=[...previews.querySelectorAll('img')];images.forEach(img=>{if(img.parentElement?.classList.contains('composer-thumb'))return;const wrap=document.createElement('span');wrap.className='composer-thumb';wrap.style.cssText='position:relative;display:block;flex:0 0 62px;width:62px;height:62px;overflow:visible;cursor:pointer';img.parentNode.insertBefore(wrap,img);wrap.append(img);const handle=document.createElement('button');handle.type='button';handle.className='composer-image-reorder';handle.setAttribute('aria-label','Переместить изображение');handle.textContent='≡';handle.style.cssText='position:absolute;left:-5px;top:-5px;width:26px;height:26px;border:0;border-radius:50%;background:rgba(28,28,30,.88);color:#fff;font-size:20px;line-height:24px;padding:0;z-index:3;box-shadow:0 1px 4px rgba(0,0,0,.35);cursor:grab;touch-action:none;user-select:none;-webkit-user-select:none';handle.addEventListener('pointerdown',event=>startDrag(event,wrap,handle));handle.addEventListener('pointermove',moveDrag);wrap.append(handle);const del=document.createElement('button');del.type='button';del.className='composer-image-delete';del.setAttribute('aria-label','Удалить изображение');del.textContent='×';del.style.cssText='position:absolute;right:-5px;top:-5px;width:24px;height:24px;border:0;border-radius:50%;background:#e5484d;color:#fff;font-size:20px;line-height:22px;padding:0;z-index:3;box-shadow:0 1px 4px rgba(0,0,0,.35)';del.addEventListener('click',event=>{event.preventDefault();event.stopPropagation();if(!confirm('Удалить это изображение?'))return;const current=[...previews.querySelectorAll('img')],index=current.indexOf(img);if(index<0||index>=files.length)return;files.splice(index,1);notifyChange()});wrap.append(del)})}
renderTelegramLayout();updateTelegramLayoutVisibility();decorate();void updateVkAspectWarning();new MutationObserver(decorate).observe(previews,{childList:true});
})();
