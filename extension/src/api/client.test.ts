import {afterEach,describe,expect,it,vi} from 'vitest';
afterEach(()=>vi.unstubAllGlobals());
describe('protected history images',()=>{it('loads image bytes through authenticatedFetch instead of a raw img request',async()=>{vi.stubGlobal('chrome',{identity:{getAuthToken:vi.fn(async()=>({token:'google-token'})),removeCachedAuthToken:vi.fn()}});const fetch=vi.fn(async()=>new Response(new Blob(['image'],{type:'image/jpeg'}),{status:200}));vi.stubGlobal('fetch',fetch);const {protectedImage}=await import('./client');const blob=await protectedImage('https://worker.example/api/images/posts%2F1.jpg');expect(blob.type).toBe('image/jpeg');expect(fetch).toHaveBeenCalledWith('http://localhost:8787/api/images/posts%2F1.jpg',expect.objectContaining({headers:expect.objectContaining({Authorization:'Bearer google-token'})}))})});
