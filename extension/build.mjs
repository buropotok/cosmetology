import {build} from 'esbuild';import {cp,mkdir,readFile,rm,writeFile} from 'node:fs/promises';
const workerBaseUrl=process.env.WORKER_BASE_URL??'http://localhost:8787',googleClientId=process.env.GOOGLE_OAUTH_CLIENT_ID??'000000000000-development.apps.googleusercontent.com',define={__WORKER_BASE_URL__:JSON.stringify(workerBaseUrl)};
await rm('dist',{recursive:true,force:true});await mkdir('dist',{recursive:true});await Promise.all([build({entryPoints:['src/content/index.ts'],bundle:true,outfile:'dist/content.js',format:'iife',target:'chrome120',define}),build({entryPoints:['src/background.ts'],bundle:true,outfile:'dist/background.js',format:'iife',target:'chrome120',define}),build({entryPoints:['src/sidepanel/index.ts'],bundle:true,outfile:'dist/sidepanel.js',format:'iife',target:'chrome120',define})]);
const manifest=JSON.parse(await readFile('manifest.json','utf8'));manifest.oauth2.client_id=googleClientId;await writeFile('dist/manifest.json',JSON.stringify(manifest,null,2));await cp('src/content/content.css','dist/content.css');await cp('src/sidepanel/index.html','dist/sidepanel.html');await cp('src/sidepanel/style.css','dist/sidepanel.css');
