export const vkMiniAppHtml = `<!doctype html><html lang="ru"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width,initial-scale=1.0"><title>Публикация VK</title><script src="https://unpkg.com/@vkontakte/vk-bridge/dist/browser.min.js"></script><style>body{font-family:sans-serif;padding:24px}button{width:100%;padding:16px;font-size:18px}pre{margin-top:20px;white-space:pre-wrap;word-break:break-word}</style></head><body><h2>Публикация в VK</h2><button id="post" disabled>Открыть публикацию</button><pre id="status">Загружаем публикацию…</pre><script>
const VK_APP_ID=54742217,VK_API_VERSION='5.199',VK_PHOTO_CONCURRENCY=4,status=document.getElementById('status'),button=document.getElementById('post');let handoff=null,currentToken='';
function handoffToken(){const query=new URLSearchParams(location.search).get('handoff');if(query)return query;const hash=new URLSearchParams(location.hash.replace(/^#/,'')).get('handoff');if(hash)return hash;const launch=new URLSearchParams(location.search).get('vk_ref')||'',match=launch.match(/(?:^|[?&#])handoff=([A-Za-z0-9_-]+)/);return match?match[1]:''}
function vkError(stage,error){const data=error&&typeof error==='object'?error:{},nested=data.error_data&&typeof data.error_data==='object'?data.error_data:{},api=nested.api_error&&typeof nested.api_error==='object'?nested.api_error:{},code=api.error_code||nested.error_code||data.error_code||'',message=api.error_msg||nested.error_reason||nested.error_msg||data.message||(error instanceof Error?error.message:'')||data.error_type||'Неизвестная ошибка',parts=['Этап: '+stage];if(data.error_type)parts.push('Тип: '+data.error_type);if(code!=='')parts.push('Код: '+code);parts.push('Сообщение: '+message);return parts.join('\\n')}
async function callVkApi(method,params){try{const result=await vkBridge.send('VKWebAppCallAPIMethod',{method,params:{...params,v:VK_API_VERSION}});if(result?.response===undefined)throw new Error('VK API не вернул результат');return result.response}catch(error){throw new Error(vkError(method,error))}}
async function prepareOnePhoto(index,auth){const server=await callVkApi('photos.getWallUploadServer',{access_token:auth.access_token,group_id:handoff.groupId});if(!server?.upload_url)throw new Error('Этап: photos.getWallUploadServer\\nСообщение: VK не вернул upload_url');let uploadResponse;try{uploadResponse=await fetch('/api/vk-handoff-upload/'+encodeURIComponent(currentToken),{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({uploadUrl:server.upload_url,index})})}catch(error){throw new Error('Этап: upload image to VK\\nСообщение: '+(error instanceof Error?error.message:String(error)))}const uploaded=await uploadResponse.json().catch(()=>null);if(!uploadResponse.ok)throw new Error('Этап: upload image to VK\\nСообщение: '+(uploaded?.error?.message||'HTTP '+uploadResponse.status));if(!uploaded?.photo||uploaded?.server===undefined||!uploaded?.hash)throw new Error('Этап: upload image to VK\\nСообщение: VK вернул неполный результат загрузки');const saved=await callVkApi('photos.saveWallPhoto',{access_token:auth.access_token,group_id:handoff.groupId,photo:uploaded.photo,server:uploaded.server,hash:uploaded.hash}),photo=Array.isArray(saved)?saved[0]:null;if(!photo?.owner_id||!photo?.id)throw new Error('Этап: photos.saveWallPhoto\\nСообщение: VK не вернул сохранённую фотографию');return 'photo'+photo.owner_id+'_'+photo.id+(photo.access_key?'_'+photo.access_key:'')}
async function prepareNativePhotoAttachments(){const count=Number(handoff.imageCount||0);if(!count)return'';status.textContent='Получаем разрешение VK на загрузку изображений…';let auth;try{auth=await vkBridge.send('VKWebAppGetAuthToken',{app_id:VK_APP_ID,scope:'photos'})}catch(error){throw new Error(vkError('VKWebAppGetAuthToken (photos)',error))}if(!auth?.access_token)throw new Error('Этап: VKWebAppGetAuthToken (photos)\\nСообщение: VK не предоставил access_token');const attachments=new Array(count);let nextIndex=0,completed=0;async function worker(){while(true){const index=nextIndex++;if(index>=count)return;attachments[index]=await prepareOnePhoto(index,auth);completed++;status.textContent='Подготовлено изображений: '+completed+' из '+count+'…'}}const workers=Array.from({length:Math.min(VK_PHOTO_CONCURRENCY,count)},()=>worker());await Promise.all(workers);return attachments.join(',')}
async function init(){try{await vkBridge.send('VKWebAppInit');currentToken=handoffToken();if(!currentToken)throw new Error('Не получен handoff token. Вернитесь в Telegram и откройте VK снова.');const response=await fetch('/api/vk-handoff/'+encodeURIComponent(currentToken),{cache:'no-store'}),result=await response.json().catch(()=>null);if(!response.ok)throw new Error(result?.error?.message||'Не удалось загрузить публикацию.');handoff=result;button.disabled=false;status.textContent='Публикация готова. Нажмите кнопку, чтобы открыть редактор VK.'}catch(error){status.textContent=error instanceof Error?error.message:String(error)}}
button.addEventListener('click',async()=>{if(!handoff)return;button.disabled=true;try{const attachment=await prepareNativePhotoAttachments();status.textContent='Открываем редактор VK…';const params={owner_id:-handoff.groupId,message:handoff.text};if(attachment)params.attachments=attachment;let result;try{result=await vkBridge.send('VKWebAppShowWallPostBox',params)}catch(error){throw new Error(vkError('VKWebAppShowWallPostBox',error))}if(result?.post_id){status.textContent='Публикация размещена. Открываем группу…';location.replace('https://vk.com/club'+handoff.groupId);return}status.textContent='VK завершил публикацию.'}catch(error){status.textContent='Ошибка VK:\\n'+(error instanceof Error?error.message:String(error))}finally{button.disabled=false}});init();
</script></body></html>`;
